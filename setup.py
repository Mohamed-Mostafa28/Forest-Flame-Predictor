from setuptools import setup, find_packages
import os

here = os.path.abspath(os.path.dirname(__file__))


setup(
    name="algerianForestFires",
    version='0.0.3',
    packages=find_packages(),
    install_requires=[],
 
)
